#include <iostream>
#include <vector>
using namespace std;
int main(){
int largo_vector, nums;
    cout<<"ingrese el largo del vector"<<endl;
    cin>>largo_vector;
    vector <int> numeros;
    cout<<"ingrese "<< largo_vector <<" datos:"<<endl;

    for(int i=0; i!= largo_vector; i++){
        cin >> nums;
        numeros.push.back(nums);
    }
    for(int i=0, i! = )
    int pos_elim= find(numeros.began(), numeros.end(), i)
}
